[build-system]
requires = ["setuptools"]

[project]
name = "pymumu"
version = "0.1.0"
description = "An API to control MuMu Player for Windows using python"
readme = "README.MD"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]